#
# config API - InfoMetis controplane
#
# The config API processes the configuration of the active repository node.
# It implements a method for each context.
#
# config stack    : processes the stack configuration
# config module   : processes the module configuration
# config solution : processes the solution configuration
#
